/**
 * DTO per un livello (es. livello di esperienza in uno sport).
 * Rappresenta un livello specifico con un identificatore e un nome.
 */
export type LivelloDTO = {
    id: number;
    nome: string;
}